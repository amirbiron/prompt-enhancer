# 🚀 Prompt Enhancer

מערכת שיפור פרומפטים בעברית עם סוכני AI מרובים.

## ✨ תכונות

- **Shadow Critic** - סוכן ביקורת שמזהה נקודות חולשה בפרומפטים
- **Category Router** - זיהוי אוטומטי של קטגוריית הפרומפט
- **Parameter Validator** - בדיקת פרמטרים חסרים לפי קטגוריה
- **Iterative Refinement** - שיפור איטרטיבי עד להגעה לציון יעד
- **Hebrew-First** - כל המערכת בנויה לעברית
- **Telegram Bot** - ממשק נוח לשימוש יומיומי
- **REST API** - אינטגרציה עם מערכות אחרות
- **Community Examples** - דוגמאות קהילתיות לשיפורים טובים

## 🏗️ ארכיטקטורה

```
┌─────────────────────────────────────────────────────────────┐
│                      User Input (Prompt)                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Category Router                           │
│              (זיהוי: code/creative/analysis...)              │
└─────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│ Parameter        │ │ Shadow Critic    │ │ (future agents)  │
│ Validator        │ │ (ביקורת פנימית)  │ │                  │
└──────────────────┘ └──────────────────┘ └──────────────────┘
              │               │               │
              └───────────────┼───────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Orchestrator                              │
│                  (מיזוג + שיפור איטרטיבי)                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Improved Prompt + Explanation             │
└─────────────────────────────────────────────────────────────┘
```

## 📦 מבנה הפרויקט

```
prompt-enhancer/
├── app.py                    # Flask app ראשי
├── bot.py                    # Telegram bot
├── config.py                 # הגדרות
├── requirements.txt          # dependencies
├── render.yaml               # Render deployment
├── .env.example              # דוגמת משתני סביבה
│
├── agents/                   # סוכני AI
│   ├── __init__.py
│   ├── shadow_critic.py      # סוכן ביקורת
│   ├── category_router.py    # זיהוי קטגוריה
│   ├── parameter_validator.py # בדיקת פרמטרים
│   └── refiner.py            # שיפור פרומפט
│
├── core/                     # לוגיקה מרכזית
│   ├── __init__.py
│   ├── models.py             # Pydantic models
│   └── orchestrator.py       # מנהל זרימה
│
├── database/                 # מסד נתונים
│   ├── __init__.py
│   └── mongodb.py            # חיבור MongoDB
│
└── templates/
    └── index.html            # ממשק Web
```

## 🚀 התקנה והרצה

### דרישות מקדימות
- Python 3.11+
- MongoDB (מקומי או Atlas)
- Gemini API Key
- Telegram Bot Token

### התקנה מקומית

```bash
# שיבוט
git clone https://github.com/yourusername/prompt-enhancer.git
cd prompt-enhancer

# סביבה וירטואלית
python -m venv venv
source venv/bin/activate  # Linux/Mac
# או: venv\Scripts\activate  # Windows

# התקנת dependencies
pip install -r requirements.txt

# הגדרת משתני סביבה
cp .env.example .env
# ערוך את .env עם הערכים שלך

# הרצה (מצב פיתוח)
python app.py
```

### פריסה ב-Render

1. **צור חשבון Render**: https://render.com

2. **צור MongoDB Atlas**:
   - https://www.mongodb.com/atlas
   - צור Cluster חינמי (M0)
   - צור Database User
   - הוסף `0.0.0.0/0` ל-Network Access
   - העתק את Connection String

3. **צור Telegram Bot**:
   - פתח @BotFather בטלגרם
   - שלח `/newbot`
   - העתק את ה-Token

4. **צור Gemini API Key**:
   - https://makersuite.google.com/app/apikey

5. **פריסה ב-Render**:
   - חבר את ה-repo ל-Render
   - הוסף Environment Variables:
     - `TELEGRAM_BOT_TOKEN`
     - `MONGODB_URI`
     - `GEMINI_API_KEY`
     - `WEBHOOK_URL` = `https://your-app.onrender.com`
   - Deploy!

6. **הגדר Webhook**:
   ```bash
   curl -X POST https://your-app.onrender.com/set-webhook
   ```

## 📡 API Reference

### POST /api/analyze
ניתוח פרומפט (בלי שיפור)

```json
// Request
{
    "prompt": "כתוב לי סקריפט פייתון",
    "user_id": "optional"
}

// Response
{
    "category": "code",
    "critique": {
        "weaknesses": [...],
        "overall_score": 5,
        "is_ready": false
    },
    "questions": ["באיזו שפת תכנות?", ...]
}
```

### POST /api/improve
שיפור מלא

```json
// Request
{
    "prompt": "כתוב לי סקריפט פייתון",
    "max_iterations": 3
}

// Response
{
    "original_prompt": "...",
    "improved_prompt": "...",
    "explanation": "...",
    "score": 8,
    "improvement_delta": 3
}
```

### POST /api/quick-critique
ביקורת מהירה (טקסט בלבד)

```json
// Request
{"prompt": "..."}

// Response
{"critique": "טקסט מעוצב בעברית"}
```

## 🤖 פקודות Telegram Bot

| פקודה | תיאור |
|--------|--------|
| `/start` | התחלה והסבר |
| `/analyze` | ניתוח בלבד |
| `/improve` | שיפור מלא |
| `/history` | היסטוריית פרומפטים |
| `/examples` | דוגמאות קהילתיות |
| `/help` | עזרה |

## 🔧 הגדרות מתקדמות

### החלפת מודל AI
בקובץ `.env`:
```env
PRIMARY_MODEL=claude  # או openai
```

### התאמת מספר איטרציות
```env
MAX_ITERATIONS=5  # ברירת מחדל: 3
```

### הוספת קטגוריות חדשות
ערוך את `agents/parameter_validator.py`:
```python
CATEGORY_PARAMETERS[PromptCategory.NEW_CATEGORY] = [
    {"name": "...", "question": "...", "importance": "required"}
]
```

## 📊 מטריקות ו-Observability

המערכת שומרת אוטומטית:
- כל פרומפט מקורי ומשופר
- ציונים לפני/אחרי
- נקודות חולשה שזוהו
- משוב משתמשים

API לסטטיסטיקות:
```bash
curl https://your-app.onrender.com/api/stats
```

## 🤝 תרומה

1. Fork את ה-repo
2. צור branch: `git checkout -b feature/amazing-feature`
3. Commit: `git commit -m 'Add amazing feature'`
4. Push: `git push origin feature/amazing-feature`
5. פתח Pull Request

## 📜 רישיון

MIT License - ראה [LICENSE](LICENSE)

## 🙏 קרדיטים

- [Google Gemini](https://deepmind.google/technologies/gemini/) - AI Models
- [python-telegram-bot](https://python-telegram-bot.org/) - Telegram Integration
- [Motor](https://motor.readthedocs.io/) - Async MongoDB Driver

---

**נבנה עם ❤️ לקהילה העברית**
